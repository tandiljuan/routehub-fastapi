ENVIRONMENT=LCL
# Used by local scripts/clients that call RouteHub (Authorization: Bearer <key>).
ROUTEHUB_API_KEY=rh_live_replace_me
OPTIMIZER_HOST=http://localhost
OPTIMIZER_PORT=3005
OPTIMIZER_AUTH=example
